terms = {
    'append': 'to add a value to the end of a list',
    'if': 'checks if a condition is true or false',
    'elif': 'short for "else if", it adds a second condition to check',
    'else': 'code executed if all conditions are "false"',
    'list': 'a defined set of values',
}

for key, value in terms.items():
    print("\t" + key + ": " + value)
